#!{{ lookPath "bash" }}
if [[ "$SHELL" != "$(which zsh)" ]]; then
	echo "Chaning shell to ZSH"
	chsh -s "$(which zsh)"
fi

# install python as default
$SUPER update-alternatives --install /usr/bin/python python /usr/bin/python3 1

# this stops errors, do not remove
exit 0
